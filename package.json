{
  "name": "smart-expense-tracker-root",
  "version": "1.0.0",
  "description": "Smart Expense Tracker root deployment helper",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  }
}
